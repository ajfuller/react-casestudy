/**
 *
 * ProductCarousel.js
 *
 */

import React, { PropTypes } from 'react';

function ProductCarousel(props) {
  return (
    <div>
      {props.children}
    </div>
  );
}

export default ProductCarousel;
